{
  "name": "yu-zora-bookshelf",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "node scripts/build.mjs && node scripts/serve.mjs",
    "preview": "node scripts/serve.mjs",
    "test": "node --test",
    "check": "node --check scripts/build.mjs && node --check scripts/content.mjs && node --check public/assets/site.js && node --check public/assets/reader.js && node --check public/assets/storage.js && node --check scripts/serve.mjs && node --check worker/index.js && node --check worker/archive-access.js",
    "build:production": "node scripts/build-production.mjs",
    "deploy:cloudflare": "npm run check && npm run build:production && npm test && wrangler deploy",
    "dev:worker": "npm run build:production && wrangler dev --local"
  },
  "devDependencies": {
    "wrangler": "4.135.0"
  }
}
